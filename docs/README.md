# Documentation

This folder keeps product and architecture references out of the active app folders.

```text
docs/
|-- TECH_STACK.md      Official STRYK V1 stack and module plan
`-- figma-export/      Original Figma-generated reference project
```

Use `figma-export/` as a visual and interaction reference only. The production frontend lives in `frontend/`.
